if(ENV STREQUAL android)
	addPkgConfigLibs(imagine "-ljnigraphics")
	target_sources(imagine PRIVATE AndroidFont.cc)
elseif(ENV STREQUAL ios)
	target_sources(imagine PRIVATE UIKitFont.mm)
else()
	addPkgFreetype(imagine)	
	if(ENV STREQUAL linux)
		addPkgFontconfig(imagine)
	endif()
	target_sources(imagine PRIVATE FreetypeFont.cc)
endif()

target_sources(imagine PRIVATE Font.cc)
